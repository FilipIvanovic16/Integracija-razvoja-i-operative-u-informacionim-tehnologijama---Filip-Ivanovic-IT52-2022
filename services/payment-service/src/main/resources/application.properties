spring.application.name=payment-service
server.port=8084

# ===================== PostgreSQL (paymentdb) =====================
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/paymentdb}
spring.datasource.username=${DB_USER:chronoshop}
spring.datasource.password=${DB_PASSWORD:chronoshop}
spring.datasource.driver-class-name=org.postgresql.Driver

# Sema je verzionisana kroz Flyway - Hibernate samo validira da se poklapa
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# ===================== REST klijent ka order-service =====================
app.order-service.url=${ORDER_SERVICE_URL:http://localhost:8083}

# ===================== RabbitMQ =====================
spring.rabbitmq.host=${RABBITMQ_HOST:localhost}
spring.rabbitmq.port=${RABBITMQ_PORT:5672}
spring.rabbitmq.username=${RABBITMQ_USER:guest}
spring.rabbitmq.password=${RABBITMQ_PASSWORD:guest}

spring.rabbitmq.listener.simple.retry.enabled=true
spring.rabbitmq.listener.simple.retry.max-attempts=3
spring.rabbitmq.listener.simple.retry.initial-interval=1000
spring.rabbitmq.listener.simple.retry.multiplier=2.0
spring.rabbitmq.listener.simple.retry.max-interval=10000

# ===================== Stripe (test mode) =====================
stripe.api.secret-key=${STRIPE_SECRET_KEY:sk_test_xxx}
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET:whsec_xxx}

# ===================== Actuator =====================
management.endpoints.web.exposure.include=health,info,prometheus
management.endpoint.health.show-details=when-authorized
